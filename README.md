# zsh-config

### oh-my-zsh config

- rename `zshrc` to `.zshrc`
- put to `~/`
- install plugins:
  - git
  - zsh-autosuggestions
  - zsh-syntax-highlighting
- run ```source ~/.zshrc ```

### iterm2 config
